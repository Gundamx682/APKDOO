# APK 下载器

一个简单的Android应用,用于自动下载APK文件到指定目录。

## 🎯 功能特性

- ✅ 自动从指定服务器下载APK
- ✅ 保存到 `/storage/emulated/0/Netamade/APK` 目录
- ✅ 实时显示下载进度
- ✅ 下载完成后自动关闭应用
- ✅ 自动请求存储权限(支持Android 11+)

## 📱 系统要求

- Android 5.0 (API 21) 及以上
- 约5MB存储空间
- 网络连接

## 🚀 GitHub Actions 自动构建

本项目已配置GitHub Actions,可以自动编译APK。

### 下载APK

1. 访问项目主页的 **Actions** 页面
2. 选择最近的构建任务
3. 在底部 **Artifacts** 区域下载:
   - `APK-Debug` - 调试版本(用于测试)
   - `APK-Release` - 正式版本

## 🔨 本地编译

### 方法1: 使用Android Studio(推荐)

1. 使用Android Studio打开此项目
2. 等待Gradle同步完成
3. 点击 **Build > Build Bundle(s) / APK(s) > Build APK(s)**
4. APK位置: `app/build/outputs/apk/debug/app-debug.apk`

### 方法2: 命令行编译

```bash
# Windows
gradlew.bat assembleDebug

# Linux/Mac
chmod +x gradlew
./gradlew assembleDebug

# APK位置: app/build/outputs/apk/debug/app-debug.apk
```

## 📦 APK信息

- **应用名称**: APK下载器
- **包名**: com.netamade.apkdownloader
- **最低版本**: Android 5.0 (API 21)
- **目标版本**: Android 14 (API 34)
- **签名**: Debug签名(测试版) / Release签名(正式版)

## 🔧 配置说明

如需修改下载地址或保存目录,编辑:
`app/src/main/java/com/netamade/apkdownloader/MainActivity.java`

```java
// 第24行:下载地址
private static final String DOWNLOAD_URL = "http://45.130.146.21:8080/xiazai";

// 第25行:保存目录
private static final String BASE_DIR = "/storage/emulated/0/Netamade/APK";
```

## 📝 使用说明

1. **安装APK** - 将编译好的APK安装到设备
2. **打开应用** - 首次打开会请求存储权限
3. **自动下载** - 应用会自动下载并显示进度
4. **自动关闭** - 下载完成后自动关闭

## 📂 项目结构

```
APKDownloader/
├── app/
│   ├── build.gradle              # 应用构建配置
│   ├── proguard-rules.pro         # ProGuard混淆规则
│   └── src/main/
│       ├── AndroidManifest.xml    # 应用清单
│       ├── java/.../MainActivity.java  # 主程序
│       └── res/                   # 资源文件
│           ├── layout/            # 布局文件
│           └── values/            # 字符串、颜色、主题
├── build.gradle                  # 项目构建配置
├── settings.gradle.kts           # Gradle设置
├── gradlew / gradlew.bat         # Gradle包装器
└── README.md                     # 说明文档
```

## 🆘 问题排查

### 构建失败
- 检查Actions日志查看具体错误
- 确保Gradle配置正确
- 检查依赖网络连接

### APK无法安装
- 确保启用"未知来源应用"
- 检查Android版本兼容性
- 尝试删除旧版本后重装

### 下载失败
- 检查网络连接
- 确认服务器地址正确: `http://45.130.146.21:8080/xiazai`
- 检查服务器是否正常运行

### 权限问题
- 确保授予存储权限
- Android 11+需要授予"所有文件访问权限"

## 📄 许可证

本项目仅供学习和个人使用。

## 📮 联系方式

如有问题或建议,请提交Issue。
